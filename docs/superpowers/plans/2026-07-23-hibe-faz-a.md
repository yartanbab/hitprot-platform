# Hibe Faz A (Katalog: GrantCall + Kriterler) Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Host'un hibe programlarına zamana bağlı çağrılar (`GrantCall`) ve eşleştirme kriterleri (sektör/bölge/anahtar kelime/ölçek) tanımlayabilmesi.

**Architecture:** ABP/DDD katmanlı. `Grant` (program) genişletilir + iki yeni entity (`GrantCall`, `GrantCriteriaTag`) eklenir. Tek migration. Çağrılar için yeni izin açılmaz — mevcut `PlatformPermissions.Grants.*` yeniden kullanılır. UI, az önce reskin edilen kart grid'ine genişletilebilir çağrı bölümü + kriter chip'leri ekler.

**Tech Stack:** .NET 10, ABP 10, EF Core (PostgreSQL), AutoMapper, Razor Pages + jQuery/DataTables, Select2, xUnit + Shouldly.

## Global Constraints

- Kullanıcıya dönen tüm metin Türkçe, `Localization/Platform/tr.json` + `en.json`'a; koda gömülmez.
- DTO↔Entity dönüşümü AutoMapper ile; elle mapping yazılmaz.
- Yeni permission tanımlanmaz (DbMigrator seed gereksinimi doğmasın) — `PlatformPermissions.Grants.Create/Edit/Delete` yeniden kullanılır.
- Tablo prefix'i `App`, şema `PlatformConsts.DbSchema`. Migration adı: `Add_GrantCall_And_Criteria`.
- Async I/O metotları `Async` ile biter; constructor injection; private field `_camelCase`.
- Her task sonunda commit (kullanıcı 2026-07-23'te "her yaptığın işi commitle" dedi). Push AYRI onay ister.
- Build/test worktree'de `abp install-libs` gerektirir; dev server ayrı portta (44398) çalışıyor olabilir — kök 44386'ya dokunma.

---

## File Structure

**Domain.Shared** (`src/Apya.Platform.Domain.Shared/Grants/`)
- `GrantCallStatus.cs` — çağrı durumu enum'u
- `CompanySize.cs` — ölçek flags enum'u
- `GrantCriteriaKind.cs` — kriter türü enum'u

**Domain** (`src/Apya.Platform.Domain/Grants/`)
- `GrantCall.cs` — yeni aggregate
- `GrantCriteriaTag.cs` — Grant'ın child'ı
- `Grant.cs` — genişletilir (EligibleCompanySizes + koleksiyonlar + davranış)

**EntityFrameworkCore**
- `EntityFrameworkCore/PlatformDbContext.cs` — DbSet + config
- `Migrations/*_Add_GrantCall_And_Criteria.cs` — üretilir

**Application.Contracts** (`src/Apya.Platform.Application.Contracts/Grants/`)
- `Dtos/GrantCallDto.cs`, `Dtos/CreateUpdateGrantCallDto.cs`, `Dtos/GetGrantCallListDto.cs`
- `Dtos/GrantCriteriaTagDto.cs`
- `Dtos/GrantDto.cs`, `Dtos/CreateUpdateGrantDto.cs` — genişletilir
- `IGrantCallAppService.cs`

**Application** (`src/Apya.Platform.Application/Grants/`)
- `GrantCallAppService.cs` — yeni
- `GrantAppService.cs` — genişletilir (kriter senkronu + CallCount)
- `ApyaPlatformApplicationAutoMapperProfile.cs` — map satırları

**Web** (`src/Apya.Platform.Web/Pages/Grants/`)
- `Index.cshtml` / `Index.js` — kriter chip'leri + genişletilebilir çağrı bölümü
- `CreateModal.cshtml(.cs)` / `EditModal.cshtml(.cs)` — kriter inputları
- `Calls/CreateModal.cshtml(.cs)` / `Calls/EditModal.cshtml(.cs)` — yeni çağrı modalları

**Tests** (`test/Apya.Platform.Domain.Tests/Grants/`)
- `GrantCall_Tests.cs`, `Grant_Criteria_Tests.cs`

---

### Task 1: Domain — enum'lar + `GrantCall` + `GrantCriteriaTag` + `Grant` genişletme

**Files:**
- Create: `src/Apya.Platform.Domain.Shared/Grants/GrantCallStatus.cs`
- Create: `src/Apya.Platform.Domain.Shared/Grants/CompanySize.cs`
- Create: `src/Apya.Platform.Domain.Shared/Grants/GrantCriteriaKind.cs`
- Create: `src/Apya.Platform.Domain/Grants/GrantCall.cs`
- Create: `src/Apya.Platform.Domain/Grants/GrantCriteriaTag.cs`
- Modify: `src/Apya.Platform.Domain/Grants/Grant.cs`
- Test: `test/Apya.Platform.Domain.Tests/Grants/GrantCall_Tests.cs`
- Test: `test/Apya.Platform.Domain.Tests/Grants/Grant_Criteria_Tests.cs`

**Interfaces:**
- Produces:
  - `enum GrantCallStatus { Planlandi=0, Acik=1, Kapandi=2 }`
  - `[Flags] enum CompanySize { Mikro=1, Kucuk=2, Orta=4, Buyuk=8 }`
  - `enum GrantCriteriaKind { Sektor=0, Bolge=1, AnahtarKelime=2 }`
  - `GrantCall(Guid id, Guid grantId, string period, GrantCallStatus status)` ctor; `void SetPeriod(string)`, `void SetSchedule(DateTime? open, DateTime? deadline)`, props `GrantId`, `Period`, `Status`, `OpenDate`, `Deadline`, `Budget`, `Reference`.
  - `GrantCriteriaTag(Guid id, Guid grantId, GrantCriteriaKind kind, string value)`; props `GrantId`, `Kind`, `Value`.
  - `Grant`: `+ int EligibleCompanySizes`, `+ ICollection<GrantCall> Calls`, `+ ICollection<GrantCriteriaTag> CriteriaTags`.

- [ ] **Step 1: enum dosyalarını oluştur**

`GrantCallStatus.cs`:
```csharp
namespace Apya.Platform.Grants;

public enum GrantCallStatus
{
    Planlandi = 0,
    Acik = 1,
    Kapandi = 2
}
```

`CompanySize.cs`:
```csharp
using System;

namespace Apya.Platform.Grants;

// Bit-maskesi: bir çağrı/program birden çok ölçeği hedefleyebilir.
// KOBİ = Mikro | Kucuk | Orta (UI yardımcı grubu; ayrı değer yok).
[Flags]
public enum CompanySize
{
    Mikro = 1,
    Kucuk = 2,
    Orta = 4,
    Buyuk = 8
}
```

`GrantCriteriaKind.cs`:
```csharp
namespace Apya.Platform.Grants;

public enum GrantCriteriaKind
{
    Sektor = 0,
    Bolge = 1,
    AnahtarKelime = 2
}
```

- [ ] **Step 2: `GrantCall.cs` oluştur**

```csharp
using System;
using Volo.Abp;
using Volo.Abp.Domain.Entities.Auditing;
using Volo.Abp.MultiTenancy;

namespace Apya.Platform.Grants;

/// <summary>
/// Bir hibe programının (Grant) zamana bağlı açılışı: dönem, son başvuru tarihi,
/// bütçe ve durum. Host katalog verisi (IMultiTenant; host'ta TenantId null).
/// </summary>
public class GrantCall : FullAuditedAggregateRoot<Guid>, IMultiTenant
{
    public Guid? TenantId { get; set; }
    public Guid GrantId { get; private set; }
    public string Period { get; private set; } = null!;
    public GrantCallStatus Status { get; set; }
    public DateTime? OpenDate { get; private set; }
    public DateTime? Deadline { get; private set; }
    public decimal? Budget { get; set; }
    public string? Reference { get; set; }

    protected GrantCall() { }

    public GrantCall(Guid id, Guid grantId, string period, GrantCallStatus status) : base(id)
    {
        GrantId = grantId;
        SetPeriod(period);
        Status = status;
    }

    public void SetPeriod(string period)
    {
        Period = Check.NotNullOrWhiteSpace(period, nameof(period), maxLength: 32).Trim();
    }

    public void SetSchedule(DateTime? openDate, DateTime? deadline)
    {
        if (openDate.HasValue && deadline.HasValue && deadline.Value < openDate.Value)
        {
            throw new BusinessException(PlatformDomainErrorCodes.GrantCallScheduleInvalid);
        }
        OpenDate = openDate;
        Deadline = deadline;
    }
}
```

- [ ] **Step 3: hata kodunu ekle**

`src/Apya.Platform.Domain.Shared/PlatformDomainErrorCodes.cs` içine yeni sabit (mevcut sabitlerin yanına, aynı `Apya.Platform:` önek desenini izleyerek):
```csharp
public const string GrantCallScheduleInvalid = "Apya.Platform:GrantCallScheduleInvalid";
```
Doğrulama: dosyada `ProjectScheduleInvalid` gibi mevcut bir sabiti bul, aynı biçimi kullan.

- [ ] **Step 4: `GrantCriteriaTag.cs` oluştur**

```csharp
using System;
using Volo.Abp;
using Volo.Abp.Domain.Entities.Auditing;
using Volo.Abp.MultiTenancy;

namespace Apya.Platform.Grants;

/// <summary>
/// Bir programın eşleştirme kriteri (sektör/bölge/anahtar kelime). Grant'ın child'ı.
/// Faz B'de FirmProfile'daki aynı türdeki etiketlerle örtüşme skoru hesaplanır.
/// </summary>
public class GrantCriteriaTag : FullAuditedAggregateRoot<Guid>, IMultiTenant
{
    public Guid? TenantId { get; set; }
    public Guid GrantId { get; private set; }
    public GrantCriteriaKind Kind { get; private set; }
    public string Value { get; private set; } = null!;

    protected GrantCriteriaTag() { }

    public GrantCriteriaTag(Guid id, Guid grantId, GrantCriteriaKind kind, string value) : base(id)
    {
        GrantId = grantId;
        Kind = kind;
        Value = Check.NotNullOrWhiteSpace(value, nameof(value), maxLength: 64).Trim();
    }
}
```

- [ ] **Step 5: `Grant.cs` genişlet**

Mevcut `Grant.cs`'e ekle (mevcut ctor'lara DOKUNMA, sadece yeni üyeler):
```csharp
using System.Collections.Generic; // dosyanın başına (yoksa)

// class gövdesine, mevcut property'lerin altına:
public int EligibleCompanySizes { get; set; } // CompanySize bit-maskesi (0 = kısıt yok)
public ICollection<GrantCall> Calls { get; set; } = new List<GrantCall>();
public ICollection<GrantCriteriaTag> CriteriaTags { get; set; } = new List<GrantCriteriaTag>();
```

- [ ] **Step 6: başarısız testleri yaz**

`GrantCall_Tests.cs`:
```csharp
using System;
using Shouldly;
using Volo.Abp;
using Xunit;
using Apya.Platform.Grants;

namespace Apya.Platform.Tests.Domain.Grants;

public class GrantCall_Tests
{
    [Fact]
    public void SetPeriod_Should_Throw_When_Blank()
    {
        var call = new GrantCall(Guid.NewGuid(), Guid.NewGuid(), "2025/1", GrantCallStatus.Acik);
        Assert.Throws<ArgumentException>(() => call.SetPeriod("  "));
    }

    [Fact]
    public void SetSchedule_Should_Throw_When_Deadline_Before_Open()
    {
        var call = new GrantCall(Guid.NewGuid(), Guid.NewGuid(), "2025/1", GrantCallStatus.Acik);
        var ex = Assert.Throws<BusinessException>(() =>
            call.SetSchedule(new DateTime(2025, 5, 10), new DateTime(2025, 5, 1)));
        ex.Code.ShouldBe(PlatformDomainErrorCodes.GrantCallScheduleInvalid);
    }

    [Fact]
    public void SetSchedule_Should_Accept_Valid_Range()
    {
        var call = new GrantCall(Guid.NewGuid(), Guid.NewGuid(), "2025/1", GrantCallStatus.Acik);
        call.SetSchedule(new DateTime(2025, 5, 1), new DateTime(2025, 6, 1));
        call.Deadline!.Value.ShouldBe(new DateTime(2025, 6, 1));
    }
}
```

`Grant_Criteria_Tests.cs`:
```csharp
using System;
using Shouldly;
using Xunit;
using Apya.Platform.Grants;

namespace Apya.Platform.Tests.Domain.Grants;

public class Grant_Criteria_Tests
{
    [Fact]
    public void CriteriaTag_Should_Trim_Value()
    {
        var tag = new GrantCriteriaTag(Guid.NewGuid(), Guid.NewGuid(), GrantCriteriaKind.Sektor, "  yazılım  ");
        tag.Value.ShouldBe("yazılım");
        tag.Kind.ShouldBe(GrantCriteriaKind.Sektor);
    }

    [Fact]
    public void CompanySize_Flags_Should_Combine()
    {
        var kobi = CompanySize.Mikro | CompanySize.Kucuk | CompanySize.Orta;
        ((int)kobi).ShouldBe(7);
        kobi.HasFlag(CompanySize.Orta).ShouldBeTrue();
        kobi.HasFlag(CompanySize.Buyuk).ShouldBeFalse();
    }
}
```

- [ ] **Step 7: testleri çalıştır — FAIL bekle**

Run: `dotnet test test/Apya.Platform.Domain.Tests --filter "FullyQualifiedName~Grants"`
Expected: derleme başarılı, yeni testler FAIL/derlenemez (kod henüz eksikse) → adımları tamamladıktan sonra tekrar.

- [ ] **Step 8: testleri çalıştır — PASS bekle**

Run: `dotnet test test/Apya.Platform.Domain.Tests --filter "FullyQualifiedName~Grants"`
Expected: PASS (5 test).

- [ ] **Step 9: commit**

```bash
git add src/Apya.Platform.Domain.Shared/Grants src/Apya.Platform.Domain/Grants src/Apya.Platform.Domain.Shared/PlatformDomainErrorCodes.cs test/Apya.Platform.Domain.Tests/Grants
git commit -m "feat(grants): GrantCall + GrantCriteriaTag domain + Grant kriter genisletme"
```

---

### Task 2: EF Core config + migration

**Files:**
- Modify: `src/Apya.Platform.EntityFrameworkCore/EntityFrameworkCore/PlatformDbContext.cs` (DbSet ~line 111 civarı, config ~line 400-408 Grant bloğu)
- Create (üretilir): `src/Apya.Platform.EntityFrameworkCore/Migrations/*_Add_GrantCall_And_Criteria.cs`

**Interfaces:**
- Consumes: Task 1 entity'leri.
- Produces: `AppGrantCalls`, `AppGrantCriteriaTags` tabloları; `AppGrants.EligibleCompanySizes` kolonu. `DbSet<GrantCall> GrantCalls`, `DbSet<GrantCriteriaTag> GrantCriteriaTags`.

- [ ] **Step 1: DbSet'leri ekle**

`PlatformDbContext.cs` içinde `public DbSet<Grant> Grants { get; set; }` satırının altına:
```csharp
public DbSet<GrantCall> GrantCalls { get; set; }
public DbSet<GrantCriteriaTag> GrantCriteriaTags { get; set; }
```

- [ ] **Step 2: Grant config bloğunu genişlet + yeni config blokları ekle**

Mevcut `builder.Entity<Grant>(b => { ... })` bloğuna `EligibleCompanySizes` satırı ekle ve hemen ardına iki yeni blok:
```csharp
builder.Entity<Grant>(b =>
{
    b.ToTable(PlatformConsts.DbTablePrefix + "Grants", PlatformConsts.DbSchema);
    b.ConfigureByConvention();
    b.Property(x => x.Name).IsRequired().HasMaxLength(128);
    b.Property(x => x.Issuer).IsRequired().HasMaxLength(64);
    b.Property(x => x.MinMatchScore).IsRequired();
    b.Property(x => x.MaxAmount).IsRequired();
    b.Property(x => x.EligibleCompanySizes).IsRequired().HasDefaultValue(0); // YENİ
});

builder.Entity<GrantCall>(b =>
{
    b.ToTable(PlatformConsts.DbTablePrefix + "GrantCalls", PlatformConsts.DbSchema);
    b.ConfigureByConvention();
    b.Property(x => x.Period).IsRequired().HasMaxLength(32);
    b.Property(x => x.Reference).HasMaxLength(64);
    b.HasOne<Grant>().WithMany(g => g.Calls).HasForeignKey(x => x.GrantId).OnDelete(DeleteBehavior.Cascade);
    b.HasIndex(x => x.GrantId);
    b.HasIndex(x => x.Deadline);
});

builder.Entity<GrantCriteriaTag>(b =>
{
    b.ToTable(PlatformConsts.DbTablePrefix + "GrantCriteriaTags", PlatformConsts.DbSchema);
    b.ConfigureByConvention();
    b.Property(x => x.Value).IsRequired().HasMaxLength(64);
    b.HasOne<Grant>().WithMany(g => g.CriteriaTags).HasForeignKey(x => x.GrantId).OnDelete(DeleteBehavior.Cascade);
    b.HasIndex(x => x.GrantId);
});
```
Gerekirse dosyanın başında `using Apya.Platform.Grants;` zaten var (line 35 civarı) — kontrol et.

- [ ] **Step 3: migration üret**

Run (EntityFrameworkCore proje dizininde):
```bash
cd src/Apya.Platform.EntityFrameworkCore
dotnet ef migrations add Add_GrantCall_And_Criteria --startup-project ../Apya.Platform.Web
```
Expected: `Migrations/<timestamp>_Add_GrantCall_And_Criteria.cs` + snapshot güncellenir.

- [ ] **Step 4: üretilen migration'ı gözden geçir**

`Up()` yalnızca: `AppGrantCalls` create, `AppGrantCriteriaTags` create, `AppGrants` ADD COLUMN `EligibleCompanySizes` (default 0) içermeli. Başka tabloya dokunmamalı. Dokunuyorsa snapshot drift var → durdur, incele.

- [ ] **Step 5: migration'ı uygula**

Run:
```bash
dotnet ef database update --startup-project ../Apya.Platform.Web
```
DbMigrator "ConnectionString not initialized" ile çökerse bu komut (EFCore proje + Web startup) zaten doğru yol.
Expected: `AppGrantCalls` + `AppGrantCriteriaTags` DB'de.

- [ ] **Step 6: pending-model-changes doğrula + build**

Run:
```bash
dotnet ef migrations has-pending-model-changes --startup-project ../Apya.Platform.Web
cd .. && dotnet build src/Apya.Platform.EntityFrameworkCore
```
Expected: "No changes"; build 0 hata.

- [ ] **Step 7: commit**

```bash
git add src/Apya.Platform.EntityFrameworkCore/EntityFrameworkCore/PlatformDbContext.cs src/Apya.Platform.EntityFrameworkCore/Migrations
git commit -m "feat(grants): GrantCall/GrantCriteriaTag EF config + migration"
```

---

### Task 3: DTO'lar + `IGrantCallAppService` (Application.Contracts)

**Files:**
- Create: `src/Apya.Platform.Application.Contracts/Grants/Dtos/GrantCriteriaTagDto.cs`
- Create: `.../Dtos/GrantCallDto.cs`, `.../Dtos/CreateUpdateGrantCallDto.cs`, `.../Dtos/GetGrantCallListDto.cs`
- Create: `src/Apya.Platform.Application.Contracts/Grants/IGrantCallAppService.cs`
- Modify: `.../Dtos/GrantDto.cs`, `.../Dtos/CreateUpdateGrantDto.cs`

**Interfaces:**
- Consumes: Task 1 enum'ları.
- Produces:
  - `GrantCriteriaTagDto { GrantCriteriaKind Kind; string Value }`
  - `GrantCallDto : EntityDto<Guid> { Guid GrantId; string Period; GrantCallStatus Status; DateTime? OpenDate; DateTime? Deadline; decimal? Budget; string? Reference; string? GrantName }`
  - `CreateUpdateGrantCallDto { Guid GrantId; string Period; GrantCallStatus Status; DateTime? OpenDate; DateTime? Deadline; decimal? Budget; string? Reference }`
  - `GetGrantCallListDto : PagedAndSortedResultRequestDto { Guid? GrantId }`
  - `IGrantCallAppService : ICrudAppService<GrantCallDto, Guid, GetGrantCallListDto, CreateUpdateGrantCallDto>`
  - `GrantDto += List<GrantCriteriaTagDto> CriteriaTags; int EligibleCompanySizes; int CallCount`
  - `CreateUpdateGrantDto += List<GrantCriteriaTagDto> CriteriaTags; int EligibleCompanySizes`

- [ ] **Step 1: `GrantCriteriaTagDto.cs`**

```csharp
namespace Apya.Platform.Grants.Dtos;

public class GrantCriteriaTagDto
{
    public GrantCriteriaKind Kind { get; set; }
    public string Value { get; set; } = string.Empty;
}
```

- [ ] **Step 2: `GrantCallDto.cs`**

```csharp
using System;
using Volo.Abp.Application.Dtos;

namespace Apya.Platform.Grants.Dtos;

public class GrantCallDto : EntityDto<Guid>
{
    public Guid GrantId { get; set; }
    public string Period { get; set; } = string.Empty;
    public GrantCallStatus Status { get; set; }
    public DateTime? OpenDate { get; set; }
    public DateTime? Deadline { get; set; }
    public decimal? Budget { get; set; }
    public string? Reference { get; set; }
    public string? GrantName { get; set; }
}
```

- [ ] **Step 3: `CreateUpdateGrantCallDto.cs`**

```csharp
using System;
using System.ComponentModel.DataAnnotations;

namespace Apya.Platform.Grants.Dtos;

public class CreateUpdateGrantCallDto
{
    [Required]
    public Guid GrantId { get; set; }

    [Required]
    [StringLength(32)]
    public string Period { get; set; } = string.Empty;

    public GrantCallStatus Status { get; set; }
    public DateTime? OpenDate { get; set; }
    public DateTime? Deadline { get; set; }
    public decimal? Budget { get; set; }

    [StringLength(64)]
    public string? Reference { get; set; }
}
```

- [ ] **Step 4: `GetGrantCallListDto.cs`**

```csharp
using System;
using Volo.Abp.Application.Dtos;

namespace Apya.Platform.Grants.Dtos;

public class GetGrantCallListDto : PagedAndSortedResultRequestDto
{
    public Guid? GrantId { get; set; }
}
```

- [ ] **Step 5: `IGrantCallAppService.cs`**

```csharp
using System;
using Volo.Abp.Application.Services;
using Apya.Platform.Grants.Dtos;

namespace Apya.Platform.Grants;

public interface IGrantCallAppService :
    ICrudAppService<GrantCallDto, Guid, GetGrantCallListDto, CreateUpdateGrantCallDto>
{
}
```

- [ ] **Step 6: `GrantDto` + `CreateUpdateGrantDto` genişlet**

`GrantDto.cs`'e ekle:
```csharp
using System.Collections.Generic; // başa

public int EligibleCompanySizes { get; set; }
public int CallCount { get; set; }
public List<GrantCriteriaTagDto> CriteriaTags { get; set; } = new();
```
`CreateUpdateGrantDto.cs`'e ekle:
```csharp
using System.Collections.Generic; // başa

public int EligibleCompanySizes { get; set; }
public List<GrantCriteriaTagDto> CriteriaTags { get; set; } = new();
```

- [ ] **Step 7: build + commit**

Run: `dotnet build src/Apya.Platform.Application.Contracts`
Expected: 0 hata.
```bash
git add src/Apya.Platform.Application.Contracts/Grants
git commit -m "feat(grants): GrantCall DTO/servis sozlesmeleri + Grant DTO kriter alanlari"
```

---

### Task 4: AppService'ler + AutoMapper (Application)

**Files:**
- Create: `src/Apya.Platform.Application/Grants/GrantCallAppService.cs`
- Modify: `src/Apya.Platform.Application/Grants/GrantAppService.cs`
- Modify: `src/Apya.Platform.Application/ApyaPlatformApplicationAutoMapperProfile.cs`

**Interfaces:**
- Consumes: Task 3 sözleşmeleri, Task 1 entity'leri.
- Produces: çalışan `GrantCallAppService` (GrantId filtreli list + GrantName doldurma); `GrantAppService` create/update'te CriteriaTags senkronu + list'te CallCount.

- [ ] **Step 1: `GrantCallAppService.cs`**

```csharp
using System;
using System.Linq;
using System.Threading.Tasks;
using Microsoft.EntityFrameworkCore;
using Volo.Abp.Application.Dtos;
using Volo.Abp.Application.Services;
using Volo.Abp.Domain.Repositories;
using Apya.Platform.Grants.Dtos;
using Apya.Platform.Permissions;

namespace Apya.Platform.Grants;

public class GrantCallAppService :
    CrudAppService<GrantCall, GrantCallDto, Guid, GetGrantCallListDto, CreateUpdateGrantCallDto>,
    IGrantCallAppService
{
    private readonly IRepository<Grant, Guid> _grantRepository;

    public GrantCallAppService(
        IRepository<GrantCall, Guid> repository,
        IRepository<Grant, Guid> grantRepository)
        : base(repository)
    {
        _grantRepository = grantRepository;
        GetPolicyName = PlatformPermissions.Grants.Default;
        GetListPolicyName = PlatformPermissions.Grants.Default;
        CreatePolicyName = PlatformPermissions.Grants.Create;
        UpdatePolicyName = PlatformPermissions.Grants.Edit;
        DeletePolicyName = PlatformPermissions.Grants.Delete;
    }

    protected override async Task<IQueryable<GrantCall>> CreateFilteredQueryAsync(GetGrantCallListDto input)
    {
        var query = await base.CreateFilteredQueryAsync(input);
        return input.GrantId.HasValue
            ? query.Where(c => c.GrantId == input.GrantId.Value)
            : query;
    }

    protected override async Task<GrantCallDto> MapToGetOutputDtoAsync(GrantCall entity)
    {
        var dto = await base.MapToGetOutputDtoAsync(entity);
        var grant = await _grantRepository.FindAsync(entity.GrantId);
        dto.GrantName = grant?.Name;
        return dto;
    }
}
```
Not: `SetSchedule` domain kuralını Create/Update'te tetiklemek için AutoMapper sonrası entity'ye `SetSchedule(OpenDate, Deadline)` çağrısı gerekir. Bunu `MapToEntityAsync`/`MapToEntity` override'ında değil — daha basiti: `CreateUpdateGrantCallDto`'da `OpenDate <= Deadline` doğrulaması UI+DataAnnotation ile yeterli DEĞİL (cross-field). Bu yüzden aşağıdaki Step 2'de override ile ele alınır.

- [ ] **Step 2: GrantCall Create/Update'te schedule kuralını uygula**

`GrantCallAppService`'e ekle (AutoMapper entity'yi kurduktan sonra domain guard'ını çağır):
```csharp
public override async Task<GrantCallDto> CreateAsync(CreateUpdateGrantCallDto input)
{
    var entity = new GrantCall(GuidGenerator.Create(), input.GrantId, input.Period, input.Status);
    entity.SetSchedule(input.OpenDate, input.Deadline);
    entity.Budget = input.Budget;
    entity.Reference = input.Reference;
    await Repository.InsertAsync(entity, autoSave: true);
    return await MapToGetOutputDtoAsync(entity);
}

public override async Task<GrantCallDto> UpdateAsync(Guid id, CreateUpdateGrantCallDto input)
{
    var entity = await Repository.GetAsync(id);
    entity.SetPeriod(input.Period);
    entity.Status = input.Status;
    entity.SetSchedule(input.OpenDate, input.Deadline);
    entity.Budget = input.Budget;
    entity.Reference = input.Reference;
    await Repository.UpdateAsync(entity, autoSave: true);
    return await MapToGetOutputDtoAsync(entity);
}
```
(Bu override'lar AutoMapper `CreateUpdateGrantCallDto→GrantCall` map'ini gereksiz kılar; yine de map, base tipler için tanımlanır — Step 5.)

- [ ] **Step 3: `GrantAppService.cs`'i genişlet — CallCount + kriter senkronu**

Mevcut `GrantAppService`'i (bugün sade `CrudAppService`) şu davranışlarla genişlet:
- `IRepository<GrantCall, Guid>` inject et; `MapToGetOutputDtoAsync`'te `CallCount = count(calls where GrantId==id)` doldur.
- `IRepository<GrantCriteriaTag, Guid>` inject et; Create/Update'te DTO'daki `CriteriaTags` ile mevcut child'ları senkronla (sil-ekle).

```csharp
private readonly IRepository<GrantCall, Guid> _callRepository;
private readonly IRepository<GrantCriteriaTag, Guid> _criteriaRepository;

// ctor'a ekle + base(repository) çağrısını koru; policy adları mevcut haliyle kalır.

protected override async Task<GrantDto> MapToGetOutputDtoAsync(Grant entity)
{
    var dto = await base.MapToGetOutputDtoAsync(entity);
    dto.CallCount = await _callRepository.CountAsync(c => c.GrantId == entity.Id);
    var tags = await _criteriaRepository.GetListAsync(t => t.GrantId == entity.Id);
    dto.CriteriaTags = tags.Select(t => new GrantCriteriaTagDto { Kind = t.Kind, Value = t.Value }).ToList();
    return dto;
}

public override async Task<GrantDto> CreateAsync(CreateUpdateGrantDto input)
{
    var dto = await base.CreateAsync(input);
    await SyncCriteriaAsync(dto.Id, input);
    return await GetAsync(dto.Id);
}

public override async Task<GrantDto> UpdateAsync(Guid id, CreateUpdateGrantDto input)
{
    var dto = await base.UpdateAsync(id, input);
    await SyncCriteriaAsync(id, input);
    return await GetAsync(id);
}

private async Task SyncCriteriaAsync(Guid grantId, CreateUpdateGrantDto input)
{
    var existing = await _criteriaRepository.GetListAsync(t => t.GrantId == grantId);
    await _criteriaRepository.DeleteManyAsync(existing, autoSave: true);
    foreach (var tag in input.CriteriaTags.Where(t => !string.IsNullOrWhiteSpace(t.Value)))
    {
        await _criteriaRepository.InsertAsync(
            new GrantCriteriaTag(GuidGenerator.Create(), grantId, tag.Kind, tag.Value), autoSave: false);
    }
    await _criteriaRepository.InsertAsync; // NOT: son autoSave için aşağıdaki nota bak
}
```
Not (kritik): yukarıdaki son satır yer tutucudur — döngüdeki son `InsertAsync`'i `autoSave: true` yapmak yerine, döngü SONRASI `await CurrentUnitOfWork.SaveChangesAsync();` çağır. `base.CreateAsync` zaten `EligibleCompanySizes`'ı AutoMapper ile yazar (Step 5 map'i bunu kapsar). `SyncCriteriaAsync` yalnız child tag'leri yönetir.

Doğru döngü sonu:
```csharp
    foreach (var tag in input.CriteriaTags.Where(t => !string.IsNullOrWhiteSpace(t.Value)))
    {
        await _criteriaRepository.InsertAsync(
            new GrantCriteriaTag(GuidGenerator.Create(), grantId, tag.Kind, tag.Value));
    }
```
(ABP repository `InsertAsync` varsayılan `autoSave:false`; UoW commit'te yazılır — açık SaveChanges gereksiz.)

- [ ] **Step 4: AutoMapper map satırları**

`ApyaPlatformApplicationAutoMapperProfile.cs`'te mevcut Grant map'lerinin (line 27-28) yanına:
```csharp
CreateMap<Grant, GrantDto>()
    .ForMember(d => d.CallCount, o => o.Ignore())
    .ForMember(d => d.CriteriaTags, o => o.Ignore()); // elle doldurulur (MapToGetOutputDtoAsync)
CreateMap<CreateUpdateGrantDto, Grant>()
    .ForMember(d => d.CriteriaTags, o => o.Ignore())
    .ForMember(d => d.Calls, o => o.Ignore()); // EligibleCompanySizes otomatik map'lenir
CreateMap<GrantCall, GrantCallDto>()
    .ForMember(d => d.GrantName, o => o.Ignore()); // MapToGetOutputDtoAsync'te doldurulur
CreateMap<CreateUpdateGrantCallDto, GrantCall>();
```
Mevcut `CreateMap<Grant, GrantDto>();` ve `CreateMap<CreateUpdateGrantDto, Grant>();` satırlarını yukarıdakiyle DEĞİŞTİR (çift map tanımı olmasın).

- [ ] **Step 5: build + AutoMapper doğrulama testi**

Run: `dotnet build src/Apya.Platform.Application`
Expected: 0 hata.
Run: `dotnet test test/Apya.Platform.Application.Tests --filter "FullyQualifiedName~AutoMapper"` (varsa ABP'nin `AbpAutoMapperOptions` config doğrulaması; yoksa Web başlatıldığında `AutoMapperConfigurationException` fırlatır — Task 5 canlı QA'da yakalanır).
Expected: PASS / 0 hata.

- [ ] **Step 6: commit**

```bash
git add src/Apya.Platform.Application/Grants src/Apya.Platform.Application/ApyaPlatformApplicationAutoMapperProfile.cs
git commit -m "feat(grants): GrantCallAppService + Grant kriter senkronu + AutoMapper"
```

---

### Task 5: Web UI — kriter inputları, çağrı yönetimi, localization

**Files:**
- Modify: `src/Apya.Platform.Web/Pages/Grants/Index.cshtml`, `Index.js`
- Modify: `src/Apya.Platform.Web/Pages/Grants/CreateModal.cshtml(.cs)`, `EditModal.cshtml(.cs)`
- Create: `src/Apya.Platform.Web/Pages/Grants/Calls/CreateModal.cshtml(.cs)`, `Calls/EditModal.cshtml(.cs)`
- Modify: `src/Apya.Platform.Domain.Shared/Localization/Platform/tr.json`, `en.json`

**Interfaces:**
- Consumes: `apya.platform.grants.grant.*` + yeni `apya.platform.grants.grantCall.*` JS proxy'leri (ABP otomatik üretir).

- [ ] **Step 1: localization anahtarları**

`tr.json`'a (mevcut `Notify:Grant:*` yanına) ekle:
```json
"GrantCall:Title": "Çağrı",
"GrantCall:Period": "Dönem",
"GrantCall:Status": "Durum",
"GrantCall:OpenDate": "Açılış Tarihi",
"GrantCall:Deadline": "Son Başvuru Tarihi",
"GrantCall:Budget": "Bütçe",
"GrantCall:Reference": "Referans No",
"GrantCall:Add": "Çağrı Ekle",
"GrantCallStatus:Planlandi": "Planlandı",
"GrantCallStatus:Acik": "Açık",
"GrantCallStatus:Kapandi": "Kapandı",
"Grant:Criteria:Sector": "Hedef Sektörler",
"Grant:Criteria:Region": "Hedef Bölgeler",
"Grant:Criteria:Keyword": "Anahtar Kelimeler",
"Grant:CompanySize": "Hedef Firma Ölçeği",
"CompanySize:Mikro": "Mikro",
"CompanySize:Kucuk": "Küçük",
"CompanySize:Orta": "Orta",
"CompanySize:Buyuk": "Büyük",
"Notify:GrantCall:Created": "Çağrı oluşturuldu.",
"Notify:GrantCall:Updated": "Çağrı güncellendi.",
"Notify:GrantCall:Deleted": "Çağrı silindi."
```
`en.json`'a aynı anahtarların İngilizcesi.

- [ ] **Step 2: program modallarına kriter inputları**

`CreateModal.cshtml` + `EditModal.cshtml` `<abp-modal-body>` sonuna ekle (ikisinde aynı):
```html
<div class="mb-2">
    <label class="form-label">@L["Grant:CompanySize"]</label>
    <div class="d-flex flex-wrap gap-3">
        <div class="form-check"><input class="form-check-input apya-size" type="checkbox" value="1" id="szMikro"><label class="form-check-label" for="szMikro">@L["CompanySize:Mikro"]</label></div>
        <div class="form-check"><input class="form-check-input apya-size" type="checkbox" value="2" id="szKucuk"><label class="form-check-label" for="szKucuk">@L["CompanySize:Kucuk"]</label></div>
        <div class="form-check"><input class="form-check-input apya-size" type="checkbox" value="4" id="szOrta"><label class="form-check-label" for="szOrta">@L["CompanySize:Orta"]</label></div>
        <div class="form-check"><input class="form-check-input apya-size" type="checkbox" value="8" id="szBuyuk"><label class="form-check-label" for="szBuyuk">@L["CompanySize:Buyuk"]</label></div>
    </div>
</div>
<div class="mb-2"><label class="form-label">@L["Grant:Criteria:Sector"]</label><select class="form-select apya-crit" data-kind="0" multiple></select></div>
<div class="mb-2"><label class="form-label">@L["Grant:Criteria:Region"]</label><select class="form-select apya-crit" data-kind="1" multiple></select></div>
<div class="mb-2"><label class="form-label">@L["Grant:Criteria:Keyword"]</label><select class="form-select apya-crit" data-kind="2" multiple></select></div>
```
`@inject IStringLocalizer<PlatformResource> L` sayfa başına (yoksa). Bu alanlar `abp-dynamic-form` DIŞINDA elle POST edilmediği için, modal JS'i (Step 4) form submit'ini yakalayıp `EligibleCompanySizes` + `CriteriaTags`'i ekleyecek. Modal cshtml.cs `Grant` view-model'ine (CreateUpdateGrantDto) bu alanlar zaten bağlı; hidden alan gerekiyorsa Step 4 doldurur.

- [ ] **Step 3: yeni çağrı modalları**

`Calls/CreateModal.cshtml` (EditModal benzeri, `Grants/CreateModal.cshtml` desenini izle):
```html
@page
@model Apya.Platform.Web.Pages.Grants.Calls.CreateModalModel
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal
@{ Layout = null; }
<abp-dynamic-form abp-model="GrantCall" asp-page="/Grants/Calls/CreateModal">
    <abp-modal>
        <abp-modal-header title="Yeni Çağrı"></abp-modal-header>
        <abp-modal-body>
            <abp-input asp-for="GrantCall.GrantId" type="hidden" />
            <abp-input asp-for="GrantCall.Period" label="Dönem (örn. 2025/1)" />
            <abp-select asp-for="GrantCall.Status" label="Durum" />
            <abp-row>
                <abp-column size="_6"><abp-input asp-for="GrantCall.OpenDate" label="Açılış Tarihi" /></abp-column>
                <abp-column size="_6"><abp-input asp-for="GrantCall.Deadline" label="Son Başvuru Tarihi" /></abp-column>
            </abp-row>
            <abp-row>
                <abp-column size="_6"><abp-input asp-for="GrantCall.Budget" label="Bütçe (₺)" /></abp-column>
                <abp-column size="_6"><abp-input asp-for="GrantCall.Reference" label="Referans No" /></abp-column>
            </abp-row>
        </abp-modal-body>
        <abp-modal-footer buttons="@(AbpModalButtons.Cancel | AbpModalButtons.Save)"></abp-modal-footer>
    </abp-modal>
</abp-dynamic-form>
```
`Calls/CreateModal.cshtml.cs`: `Grants/CreateModal.cshtml.cs` desenini birebir izle; `[BindProperty] public CreateUpdateGrantCallDto GrantCall { get; set; }`, `OnGetAsync(Guid grantId)` ile `GrantCall = new() { GrantId = grantId, Status = GrantCallStatus.Acik }`, `OnPostAsync()` → `_grantCallAppService.CreateAsync(GrantCall)`. `EditModal` aynı; `OnGetAsync(Guid id)` ile mevcut çağrıyı yükle, `OnPostAsync` → `UpdateAsync`.

- [ ] **Step 4: `Index.js` — kriter senkronu + genişletilebilir çağrı bölümü**

`tileTemplate`'e ekle: kriter chip'leri (CriteriaTags Kind'a göre gruplu `apya-chip`), ölçek chip'leri (EligibleCompanySizes maskesinden), "Çağrılar (CallCount)" genişletilebilir link (Webhooks `Index.js` `.apya-toggle-logs` desenini birebir kopyala: aç/kapa + lazy `grantCall.getList({grantId, maxResultCount:100})`). Çağrı satırında durum rozeti + son tarih + düzenle/sil ikonları + "Çağrı Ekle".
Program modalı için Select2 tags init (Tasks modal deseni) + submit yakalama: modal formu submit olurken `.apya-size` checkbox'larından maske hesapla, `.apya-crit` select'lerden `CriteriaTags` (Kind=data-kind, Value=her etiket) dizisini kur, DTO'ya ekle (ABP dynamic-form JSON POST'una hidden input veya `abp.ajax` override ile — Documents island'ındaki antiforgery/`abp.ajax` deseni). EditModal açılışında mevcut `grant.criteriaTags` + `eligibleCompanySizes` ile Select2/checkbox'ları ön-doldur.

Kod hacmi büyük olduğundan referans desenler:
- Genişletilebilir bölüm + lazy load: `Pages/DynamicAssets/Webhooks/Index.js:50-63,222-237`.
- Select2 tags + dark köprü + dropdownParent: Tasks `EditModal.cshtml`/`index.js` Select2 init.
- `abp.ajax` + antiforgery (custom POST): `wwwroot/dynamic-assets/src/documents.jsx` upload deseni.

- [ ] **Step 5: canlı QA (ayrı port, host oturumu)**

Sunucu (44398) ayakta değilse: `powershell -ExecutionPolicy Bypass -File scripts/dev-up.ps1 -WebPort 44398`.
Claude Browser (`javascript_tool` + `get_page_text`; screenshot timeout'a takılırsa DOM/computed-style):
- Program oluştur: ölçek Mikro+Küçük seç + 2 sektör + 1 bölge etiketi → kaydet → sayfa yenile → chip'ler + ölçekler doğru round-trip.
- Programa çağrı ekle (Dönem 2025/1, Durum Açık, Son Tarih) → "Çağrılar (1)" genişlet → satır doğru (durum rozeti + son tarih).
- Çağrı düzenle (son tarih değiştir) → düzenle (deadline<open denemesi → BusinessException/hata bildirimi) → sil.
- CallCount kartta güncel; boş-durum doğru; dark/light + mobil taşma yok; konsol temiz (yalnız SignalR 405).
- Test verisi temizle.

- [ ] **Step 6: build + commit**

Run: `dotnet build src/Apya.Platform.Web/Apya.Platform.Web.csproj`
Expected: 0 hata.
```bash
git add src/Apya.Platform.Web/Pages/Grants src/Apya.Platform.Domain.Shared/Localization/Platform/tr.json src/Apya.Platform.Domain.Shared/Localization/Platform/en.json
git commit -m "feat(grants): host katalog UI - kriter inputlari + cagri yonetimi"
```

---

## Self-Review notları

- **Spec kapsamı:** GrantCall (Task 1-2), Grant kriterleri (Task 1-2), DTO/servis (Task 3-4), host UI (Task 5), localization (Task 5), doğrulama (her task). Tümü karşılanıyor.
- **Faz A DIŞI korundu:** FirmProfile/eşleştirme/başvuru yok; çağrı-seviyesi kriter override yok.
- **Tip tutarlılığı:** `CreateUpdateGrantCallDto`, `GetGrantCallListDto`, `GrantCriteriaTagDto` tüm task'larda aynı imza. `EligibleCompanySizes` her yerde `int` maske. `GrantCallStatus`/`GrantCriteriaKind`/`CompanySize` enum adları tutarlı.
- **Bilinen risk (Task 4 Step 3):** ABP `InsertAsync` autoSave semantiği — plan döngü sonunda açık SaveChanges'e gerek olmadığını belirtir (UoW commit'te yazılır); implementasyonda `GrantAppService`'in mevcut `base(repository)` policy adları korunmalı.
