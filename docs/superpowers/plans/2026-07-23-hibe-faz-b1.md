# Hibe Faz B1 (Firma Profili + Eşleştirme + Öneri Feed + Başvur) Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Tenant firmalar profillerini doldurup profil-uyumlu açık çağrıları skorlu görebilsin ve "Başvur" ile minimal başvuru oluşturabilsin. Host katalog görünümü değişmez.

**Architecture:** `/Grants` bağlama-duyarlı (host=Faz A katalog; tenant=profil+öneri). Kural-bazlı `GrantMatchManager` genişletilebilir `FirmSignals` girdisiyle (B1'de profil-only). Katalog (Grant/GrantCall) host'ta `TenantId=null` → tenant okurken `IDataFilter<IMultiTenant>.Disable()`.

**Tech Stack:** .NET 10, ABP 10, EF Core (PostgreSQL), AutoMapper, Razor Pages + jQuery, xUnit + Shouldly.

## Global Constraints
- Kullanıcı metni TR, `Localization/Platform/*.json`; koda gömme (mevcut Grants JS/cshtml satır-içi TR stiline uy).
- Yeni permission YOK — `PlatformPermissions.Grants.Default/Create/...` yeniden kullan.
- DTO↔Entity AutoMapper; elle map yazma. GERÇEK profil dosyası `PlatformApplicationAutoMapperProfile.cs` (Apya-prefix'li dosya ölü `Class1` stub).
- Tablo prefix `App`, şema `PlatformConsts.DbSchema`. Migration: `Add_FirmProfile_And_Application`.
- Her task sonu commit; push beklemede (kullanıcı "A/B bitene kadar tut").
- Build/test worktree'de `abp install-libs`. EF/migration öncesi 44398 dev server'ı DURDUR (DLL kilidi).
- `GrantMatchManager` genişletilebilir kalsın (B2 proje sinyalleri FirmSignals'a eklenecek) — skor boyutları eklemeli.

---

## File Structure
- Domain.Shared: `Grants/GrantApplicationStage.cs`
- Domain: `Grants/FirmProfile.cs`, `Grants/FirmProfileTag.cs`, `Grants/GrantApplication.cs`, `Grants/FirmSignals.cs`, `Grants/GrantMatchManager.cs`
- EntityFrameworkCore: `PlatformDbContext.cs` + migration
- Application.Contracts: `Grants/Dtos/FirmProfileDto.cs`, `UpdateFirmProfileDto.cs`, `GrantRecommendationDto.cs`, `GrantApplicationDto.cs`; `IFirmProfileAppService.cs`, `IGrantRecommendationAppService.cs`, `IGrantApplicationAppService.cs`
- Application: `Grants/FirmProfileAppService.cs`, `GrantRecommendationAppService.cs`, `GrantApplicationAppService.cs`, AutoMapper satırları
- Web: `Pages/Grants/Index.cshtml` (host/tenant dallanma), `Index.js` (host, değişmez), `Tenant.js` (yeni tenant görünümü)
- Tests: `test/Apya.Platform.Domain.Tests/Grants/GrantMatchManager_Tests.cs`

---

### Task 1: Domain — profil/başvuru entity'leri + GrantMatchManager + testler

**Files:** yukarıdaki Domain.Shared + Domain + test.

- [ ] **Step 1: `GrantApplicationStage.cs`** (`Domain.Shared/Grants/`)
```csharp
namespace Apya.Platform.Grants;

public enum GrantApplicationStage
{
    Basvuru = 0,
    Degerlendirme = 1,
    Onay = 2,
    Odeme = 3
}
```

- [ ] **Step 2: `FirmProfile.cs` + `FirmProfileTag.cs`** (`Domain/Grants/`)
```csharp
// FirmProfile.cs
using System;
using System.Collections.Generic;
using Volo.Abp.Domain.Entities.Auditing;
using Volo.Abp.MultiTenancy;

namespace Apya.Platform.Grants;

/// <summary>Firma (tenant) eşleştirme profili — tenant başına tekil.</summary>
public class FirmProfile : FullAuditedAggregateRoot<Guid>, IMultiTenant
{
    public Guid? TenantId { get; set; }
    public CompanySize? Size { get; set; }
    public ICollection<FirmProfileTag> Tags { get; set; } = new List<FirmProfileTag>();

    protected FirmProfile() { }
    public FirmProfile(Guid id, Guid? tenantId) : base(id) { TenantId = tenantId; }
}
```
```csharp
// FirmProfileTag.cs — GrantCriteriaTag deseni
using System;
using Volo.Abp;
using Volo.Abp.Domain.Entities.Auditing;
using Volo.Abp.MultiTenancy;

namespace Apya.Platform.Grants;

public class FirmProfileTag : FullAuditedAggregateRoot<Guid>, IMultiTenant
{
    public Guid? TenantId { get; set; }
    public Guid FirmProfileId { get; private set; }
    public GrantCriteriaKind Kind { get; private set; }
    public string Value { get; private set; } = null!;

    protected FirmProfileTag() { }
    public FirmProfileTag(Guid id, Guid firmProfileId, GrantCriteriaKind kind, string value) : base(id)
    {
        FirmProfileId = firmProfileId; Kind = kind;
        Value = Check.NotNullOrWhiteSpace(value, nameof(value), maxLength: 64).Trim();
    }
}
```

- [ ] **Step 3: `GrantApplication.cs`** (`Domain/Grants/`, minimal)
```csharp
using System;
using Volo.Abp.Domain.Entities.Auditing;
using Volo.Abp.MultiTenancy;

namespace Apya.Platform.Grants;

/// <summary>Firma başvurusu (minimal). Faz C: ApprovedAmount + dilim + milestone.</summary>
public class GrantApplication : FullAuditedAggregateRoot<Guid>, IMultiTenant
{
    public Guid? TenantId { get; set; }
    public Guid GrantCallId { get; private set; }
    public GrantApplicationStage Stage { get; set; }
    public DateTime AppliedDate { get; private set; }

    protected GrantApplication() { }
    public GrantApplication(Guid id, Guid? tenantId, Guid grantCallId) : base(id)
    {
        TenantId = tenantId; GrantCallId = grantCallId;
        Stage = GrantApplicationStage.Basvuru; AppliedDate = DateTime.Now;
    }
}
```

- [ ] **Step 4: `FirmSignals.cs`** (`Domain/Grants/`, genişletilebilir eşleştirici girdisi)
```csharp
using System.Collections.Generic;

namespace Apya.Platform.Grants;

/// <summary>Eşleştirici girdisi. B1: profil sinyalleri. B2: proje sinyalleri (bütçe/kategori/kapasite) eklenecek.</summary>
public sealed class FirmSignals
{
    public CompanySize? Size { get; set; }
    public IReadOnlyList<FirmSignalTag> Tags { get; set; } = new List<FirmSignalTag>();
    // B2: public decimal? TypicalProjectBudget; public ProjectCategory? DominantCategory; public int ActiveProjectCount;
}

public sealed record FirmSignalTag(GrantCriteriaKind Kind, string Value);
```

- [ ] **Step 5: `GrantMatchManager.cs`** (`Domain/Grants/`)
```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using Volo.Abp.Domain.Services;

namespace Apya.Platform.Grants;

/// <summary>
/// Kural-bazlı firma↔program uyum skoru (0-100). Genişletilebilir: yeni skor boyutları
/// (B2 proje sinyalleri) buraya eklenir. Saf hesap, kalıcılık yok.
/// </summary>
public class GrantMatchManager : DomainService
{
    public int Score(FirmSignals firm, Grant grant, IReadOnlyList<GrantCriteriaTag> grantTags)
    {
        var byKind = grantTags.GroupBy(t => t.Kind).ToList();
        double @base;
        if (byKind.Count == 0)
        {
            @base = 0; // hedeflenmemiş program eşleşmez (host etiketlemeli)
        }
        else
        {
            var firmByKind = firm.Tags
                .GroupBy(t => t.Kind)
                .ToDictionary(g => g.Key, g => g.Select(x => Norm(x.Value)).ToHashSet());
            var dims = new List<double>();
            foreach (var g in byKind)
            {
                var grantValues = g.Select(x => Norm(x.Value)).ToHashSet();
                var firmValues = firmByKind.TryGetValue(g.Key, out var fv) ? fv : new HashSet<string>();
                var matched = grantValues.Count(v => firmValues.Contains(v));
                dims.Add(grantValues.Count == 0 ? 0 : (double)matched / grantValues.Count);
            }
            @base = dims.Average() * 100.0;
        }

        var sizeOk = grant.EligibleCompanySizes == 0
                     || firm.Size == null
                     || ((int)firm.Size.Value & grant.EligibleCompanySizes) != 0;
        var score = sizeOk ? @base : @base * 0.3;
        return (int)Math.Round(Math.Clamp(score, 0, 100));
    }

    public bool IsRecommended(FirmSignals firm, Grant grant, IReadOnlyList<GrantCriteriaTag> grantTags)
        => Score(firm, grant, grantTags) >= grant.MinMatchScore;

    private static string Norm(string s) => (s ?? string.Empty).Trim().ToLowerInvariant();
}
```

- [ ] **Step 6: testler** `GrantMatchManager_Tests.cs` (saf new'lenebilir, ABP fixture gerekmez — Project_Tests deseni)
```csharp
using System;
using System.Collections.Generic;
using Shouldly;
using Xunit;
using Apya.Platform.Grants;

namespace Apya.Platform.Tests.Domain.Grants;

public class GrantMatchManager_Tests
{
    private readonly GrantMatchManager _m = new();

    private static Grant Grant(int sizes, double minScore) =>
        new Grant(Guid.NewGuid(), "P", "K", 1000000m, minScore) { EligibleCompanySizes = sizes };

    private static List<GrantCriteriaTag> Tags(params (GrantCriteriaKind, string)[] xs)
    {
        var gid = Guid.NewGuid();
        var list = new List<GrantCriteriaTag>();
        foreach (var (k, v) in xs) list.Add(new GrantCriteriaTag(Guid.NewGuid(), gid, k, v));
        return list;
    }

    private static FirmSignals Firm(CompanySize? size, params (GrantCriteriaKind, string)[] xs) =>
        new FirmSignals { Size = size, Tags = xs.Select(x => new FirmSignalTag(x.Item1, x.Item2)).ToList() };

    [Fact]
    public void Full_Overlap_Scores_100_When_Size_Ok()
    {
        var grant = Grant((int)CompanySize.Kucuk, 50);
        var gt = Tags((GrantCriteriaKind.Sektor, "yazılım"));
        var firm = Firm(CompanySize.Kucuk, (GrantCriteriaKind.Sektor, "Yazılım"));
        _m.Score(firm, grant, gt).ShouldBe(100);
    }

    [Fact]
    public void No_Grant_Tags_Scores_Zero()
    {
        _m.Score(Firm(CompanySize.Kucuk), Grant(0, 0), new List<GrantCriteriaTag>()).ShouldBe(0);
    }

    [Fact]
    public void Partial_Overlap_Averaged_Across_Kinds()
    {
        var grant = Grant(0, 0);
        var gt = Tags((GrantCriteriaKind.Sektor, "a"), (GrantCriteriaKind.Sektor, "b"),
                      (GrantCriteriaKind.Bolge, "ankara"));
        // sektör 1/2 = .5, bölge 1/1 = 1 → ort .75 → 75
        var firm = Firm(null, (GrantCriteriaKind.Sektor, "a"), (GrantCriteriaKind.Bolge, "Ankara"));
        _m.Score(firm, grant, gt).ShouldBe(75);
    }

    [Fact]
    public void Size_Mismatch_Applies_Penalty()
    {
        var grant = Grant((int)CompanySize.Buyuk, 0);
        var gt = Tags((GrantCriteriaKind.Sektor, "a"));
        var firm = Firm(CompanySize.Mikro, (GrantCriteriaKind.Sektor, "a")); // base 100, size uyumsuz → 30
        _m.Score(firm, grant, gt).ShouldBe(30);
    }

    [Fact]
    public void IsRecommended_Uses_MinMatchScore()
    {
        var grant = Grant(0, 80);
        var gt = Tags((GrantCriteriaKind.Sektor, "a"), (GrantCriteriaKind.Sektor, "b"));
        var firm = Firm(null, (GrantCriteriaKind.Sektor, "a")); // 50 < 80
        _m.IsRecommended(firm, grant, gt).ShouldBeFalse();
    }
}
```
(Test dosyasına `using System.Linq;` ekle.)

- [ ] **Step 7:** `dotnet test test/Apya.Platform.Domain.Tests --filter GrantMatchManager` → PASS (5). Commit:
```
git add src/Apya.Platform.Domain.Shared/Grants src/Apya.Platform.Domain/Grants test/.../GrantMatchManager_Tests.cs
git commit -m "feat(grants): FirmProfile/GrantApplication domain + GrantMatchManager (B1)"
```

---

### Task 2: EF config + migration
**Files:** `PlatformDbContext.cs` + migration.

- [ ] DbSet: `FirmProfiles`, `FirmProfileTags`, `GrantApplications`.
- [ ] Config (Faz A GrantCall/GrantCriteriaTag deseni):
  - `AppFirmProfiles`: ConfigureByConvention; `HasIndex(x=>x.TenantId).IsUnique()` (tenant başına tekil).
  - `AppFirmProfileTags`: `Value` required(64); `HasOne<FirmProfile>().WithMany(p=>p.Tags).HasForeignKey(x=>x.FirmProfileId).OnDelete(Cascade)`; index FirmProfileId.
  - `AppGrantApplications`: `HasOne<GrantCall>().WithMany().HasForeignKey(x=>x.GrantCallId).OnDelete(Cascade)`; `HasIndex(x=>new{x.TenantId,x.GrantCallId}).IsUnique()`.
- [ ] 44398 dev server DURDUR. Migration `Add_FirmProfile_And_Application` üret → Up() yalnız 3 yeni tablo (+ indexler) içermeli, gözden geçir → uygula → `has-pending-model-changes` temiz → build EFCore 0 hata.
- [ ] Commit: `feat(grants): FirmProfile/GrantApplication EF config + migration (B1)`

---

### Task 3: Contracts (Application.Contracts/Grants)
DTO'lar + arayüzler. Tag'ler için mevcut `GrantCriteriaTagDto` (Kind+Value) yeniden kullanılır.

- [ ] `FirmProfileDto { CompanySize? Size; List<GrantCriteriaTagDto> Tags }`
- [ ] `UpdateFirmProfileDto { CompanySize? Size; List<GrantCriteriaTagDto> Tags }`
- [ ] `GrantRecommendationDto { Guid GrantCallId; Guid GrantId; string GrantName; string Issuer; string Period; DateTime? Deadline; int? DaysRemaining; decimal? MaxAmount; int Score; bool AlreadyApplied }`
- [ ] `GrantApplicationDto : EntityDto<Guid> { Guid GrantCallId; string? Period; string? GrantName; GrantApplicationStage Stage; DateTime AppliedDate }`
- [ ] `IFirmProfileAppService : IApplicationService { Task<FirmProfileDto> GetMyProfileAsync(); Task<FirmProfileDto> UpdateMyProfileAsync(UpdateFirmProfileDto input); }`
- [ ] `IGrantRecommendationAppService : IApplicationService { Task<List<GrantRecommendationDto>> GetMyRecommendationsAsync(); }`
- [ ] `IGrantApplicationAppService : IApplicationService { Task<GrantApplicationDto> ApplyAsync(Guid grantCallId); Task<List<GrantApplicationDto>> GetMyApplicationsAsync(); }`
- [ ] Build Contracts 0 hata. Commit: `feat(grants): B1 tenant DTO/servis sozlesmeleri`

---

### Task 4: Application (tenant AppService'ler + AutoMapper)
**Kritik:** katalog (Grant/GrantCall/GrantCriteriaTag) host'ta `TenantId=null`. Tenant okurken `IDataFilter<IMultiTenant>.Disable()` (idiom: TaskDeadlineWorker/NotificationAppService).

- [ ] `FirmProfileAppService : ApplicationService, IFirmProfileAppService` — `[Authorize(PlatformPermissions.Grants.Default)]`.
  - `GetMyProfileAsync`: `CurrentTenant.Id` için FirmProfile+Tags bul (yoksa boş `FirmProfileDto`).
  - `UpdateMyProfileAsync`: upsert (yoksa `new FirmProfile(GuidGenerator.Create(), CurrentTenant.Id)`), Size ata, Tags senkronla (sil-ekle, Faz A `SyncCriteriaAsync` deseni), döndür.
- [ ] `GrantRecommendationAppService` — `[Authorize(PlatformPermissions.Grants.Default)]`, inject `IRepository<GrantCall>`, `IRepository<Grant>`, `IRepository<GrantCriteriaTag>`, `IRepository<FirmProfile>`, `IRepository<FirmProfileTag>`, `IRepository<GrantApplication>`, `GrantMatchManager`, `IDataFilter<IMultiTenant>`.
  - Profil→FirmSignals (Size + tags).
  - `using (_mtFilter.Disable())` içinde: açık çağrılar (`Status==Acik`) + programları + kriter tag'leri (batch). Her çağrının programını skorla; `score >= grant.MinMatchScore` olanları al.
  - `AlreadyApplied`: tenant'ın GrantApplication'larından (filtre AÇIK, tenant-scoped) GrantCallId set'i.
  - DaysRemaining = deadline - bugün (null-safe). Skora göre azalan sırala. Döndür.
- [ ] `GrantApplicationAppService` — `[Authorize(PlatformPermissions.Grants.Default)]`.
  - `ApplyAsync(grantCallId)`: tenant için mevcut başvuru var mı (idempotent) → varsa döndür; yoksa `new GrantApplication(GuidGenerator.Create(), CurrentTenant.Id, grantCallId)` insert. (Çağrı geçerliliği: filter-disable ile GrantCall var mı kontrol; yoksa `EntityNotFound`.)
  - `GetMyApplicationsAsync`: tenant başvuruları; her biri için (filter-disable) call.Period + grant.Name doldur.
- [ ] AutoMapper (`PlatformApplicationAutoMapperProfile.cs`): `FirmProfile→FirmProfileDto` (Tags/Size), `GrantApplication→GrantApplicationDto` (Period/GrantName ignore, elle doldur). Recommendation DTO elle kurulur (map yok).
- [ ] Build Application 0 hata + `dotnet test ...Sample` (modül boot). Commit: `feat(grants): FirmProfile/Recommendation/Application AppService (B1)`

---

### Task 5: UI /Grants bağlama-duyarlı + localization + canlı QA
- [ ] `IndexModel` (`ICurrentTenant` inject): `public bool IsHost => CurrentTenant.Id == null;`
- [ ] `Index.cshtml`: `@if (Model.IsHost) { <mevcut Faz A katalog + modallar> } else { <tenant görünüm kabuğu> }`.
  - Tenant kabuğu: "Firma Profili" kartı (ölçek select + 3 chip-input; Faz A `.apya-tag-input` widget'ı) + Kaydet; "Önerilen Çağrılar" grid (`#RecoGrid`); "Başvurularım" (`#AppsList`). Host'ta `Index.js`, tenant'ta `Tenant.js` yüklenir (`@if` ile script seçimi).
- [ ] `Tenant.js` (yeni): profil yükle/kaydet (`firmProfile.getMyProfile/updateMyProfile`), öneri feed (`grantRecommendation.getMyRecommendations` → skor rozeti+kalan gün+Başvur), Başvur (`grantApplication.apply` → notify + AlreadyApplied), Başvurularım (`grantApplication.getMyApplications`). Chip-input helper'ları Faz A Index.js'ten uyarlanır.
- [ ] Localization: gerekli yeni TR/EN anahtarları (aşama adları vb.) — satır-içi TR stiline uyulacaksa minimal.
- [ ] Build Web 0 hata.
- [ ] **Canlı QA — TENANT bağlamı:** 44398 başlat. Tenant'a geç (header tenant switch / tenant kullanıcı girişi; seed'de 3 tenant). Profil doldur (ölçek+etiket) → kaydet → öneri feed skorlu/eşikli görünür (host katalogdaki kriterlere göre) → Başvur → Başvurularım'da görünür, tekrar-başvuru idempotent. **Host bağlamı hâlâ katalog gösterir (regresyon yok).** Dark/light + mobil. Test verisi temizle.
- [ ] Commit: `feat(grants): /Grants tenant gorunumu - profil + oneri feed + basvur (B1)`

---

## Self-Review
- **Çok-kiracılık:** katalog `TenantId=null` → tenant okumada `IDataFilter.Disable()` ŞART (yoksa öneri feed boş çıkar — sessiz hata). FirmProfile/GrantApplication tenant-scoped kalır (filter AÇIK).
- **Genişletilebilirlik:** FirmSignals + GrantMatchManager boyut-eklemeli; B2 proje sinyalleri (bütçe/kategori/kapasite) yalnız FirmSignals'a alan + Score'a boyut ekler, imza kırılmaz.
- **İdempotent Başvur:** unique index (TenantId, GrantCallId) + AppService kontrolü çift başvuruyu önler.
- **Tip tutarlılığı:** GrantCriteriaTagDto profil tag'leri için yeniden kullanılıyor; GrantApplicationStage tüm katmanlarda aynı.
